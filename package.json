{
  "name": "memory-optimizer",
  "version": "0.9",
  "description": "Transparent token compression for Newmax AI conversations",
  "main": "dist/scripts/index.js",
  "types": "dist/scripts/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "vitest",
    "clean": "rm -rf dist",
    "prepublish": "npm run build"
  },
  "keywords": [
    "newmax",
    "memory",
    "token-optimization",
    "agent",
    "mermaid"
  ],
  "author": "Se7en",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.8.0",
    "typescript": "^5.9.3",
    "vitest": "^1.0.0"
  },
  "peerDependencies": {
    "newmax-core": ">=2.5.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
